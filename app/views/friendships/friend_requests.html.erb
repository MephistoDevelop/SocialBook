<div class="container">
<h3>Friends Requests</h3>
  <% current_user.pending_requests.each do |friendship| %>
  <% if !current_user.pending_requests.empty?%>
      <h5> <%= button_to "Accept Request From #{ display_friend_requests(friendship) }", {:controller => "friendships",:friend_o => accept_friend_now(friendship), :action => "accept_friend"} , :method=>:post,:class=>"btn btn-friends"  %>  </h5>
   <div class="container"> </div>
  <% else%>
      <h4>You dont have friends Requests !</h4>
  <% end %>
  <% end %>
</div>
